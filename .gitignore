# Flutter
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
build/
pubspec.lock

# Node (relay + plugin)
server/node_modules/
plugin/node_modules/
.bridge-frames/

# Session memory (never commit)
.memory/

# IDE / OS
.idea/
*.iml
.vscode/
.DS_Store

# Android keystores (only the .b64 snapshot is committed)
**/*.keystore
android/keystore/*.jks
**/*.jks

# Local
*.log